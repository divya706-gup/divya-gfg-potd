class Solution {
public:
    int longestSubseq(vector<int>& arr) {
        unordered_map<int, int> mp;
        int ans = 1;

        for (int x : arr) {
            mp[x] = 1 + max(mp[x + 1], mp[x - 1]);
            ans = max(ans, mp[x]);
        }

        return ans;
    }
};
